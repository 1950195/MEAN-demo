<!doctype html>
<html ng-app="adminCtrls">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    {{> index.top.assets}}
</head>
<body>
    {{> navigation}}
    <div class="container-fluid">
        <div class="row" ng-controller="menuCtrl">
            {{> sidebar}}
            <div ng-view class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
            </div>
        </div>
    </div>
    {{> index.bottom.assets}}
</body>
</html>
